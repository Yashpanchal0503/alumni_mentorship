{
  "name": "alumni-mentorship",
  "version": "1.0.0",
  "description": "Alumni Mentorship Platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:seed": "npm run prisma:seed --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:push": "npm run prisma:push --prefix backend",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run prisma:migrate && npm run prisma:seed"
  },
  "devDependencies": {
    "@types/morgan": "^1.9.10",
    "concurrently": "^8.2.2"
  }
}
